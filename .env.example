# 이 파일을 복사해서 .env 로 만들고 실제 값을 채우세요.
#   cp .env.example .env
# .env 는 .gitignore 에 있어 저장소에 올라가지 않습니다.

# 32바이트(영문 기준 32자) 이상이어야 합니다. 아래 명령으로 만들 수 있습니다.
#   openssl rand -base64 48
JWT_SECRET=

# 데모용 IoT 측정값 자동 생성기. 끄고 싶으면 false 로 바꾸세요.
SIMULATOR_ENABLED=true
SIMULATOR_INTERVAL_MS=20000
